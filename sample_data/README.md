# Sample data for testing

- **dem_30m_sample.tif** — Small 30m DEM (256×256 px) for quick inference tests.  
  Create it with: `python scripts/create_sample_dem.py` (see [TESTING.md](../TESTING.md)).

You can also place your own 30m GeoTIFF here and use it as `--input` for inference.
