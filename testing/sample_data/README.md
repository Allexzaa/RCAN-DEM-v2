# Sample data

- **dem_30m_sample.tif** — 256x256 px 30m DEM (included). Use as `--input testing/sample_data/dem_30m_sample.tif`.
- Regenerate: from repo root run `python testing/create_sample_dem.py`.
